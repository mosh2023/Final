# Слово тимлида, прод на асфальте.

Победа будет за нами. Она непременно будет за нами.

## Как запустить проект

```bash
python -m venv venv

source venv/bin/activate

pip install -r requirements.txt

python run.py
```
